#include "headers.h"
#include "str_length.h"


char *trimmed_input(char *n_record, char *entry_value)

char trimmed_input;
char numkey;
char value

int i;

i = 0;
j = 0;
l = str_length(str2);
while (n_record[i] != '\0' && != ':')
{
	if (n_record[i] == ' ')
	{
		i++;
	}
	return (0);
}
numkey = &n_record[];
while (entry_value[j] != '\0';
{
	if ((entry_value[j] == ' ')
	{
		j++,
		else
		{
			entry_value[l] - 1
			while (entry_value[l] == ' ')
			{
				l--;
			}
		}
	}
}
value = &entry_value[];

return (0;
}
