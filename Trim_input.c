#include "headers.h"
#include "str_length.h"

char *trimmed_input(char *n_record, char *entry_value)
{
char *numkey;
char value
int i;

i = 0;
j = 0;
l = str_length(str2);
while (n_record[i] != '\0' && != ':')
{
	if (n_record[i] == ' ')
	{
		i++;
	}
}
numkey = &n_record[];

return (numkey);
}
while (entry_value[j] != '\0';
{
	if ((entry_value[j] == ' ')
	{
		j++,
		else
		{
			entry_value[l] - 1
			while (entry_value[l] == ' ')
			{
				l--;
			}
		}
	}
}
value = &entry_value[];

return (value);
}
